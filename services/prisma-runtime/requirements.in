# Direct dependencies of the Prisma Local runtime. Human-editable.
# The full transitive graph is pinned with hashes in requirements.lock.txt.
# See README.md for the lock regeneration procedure.
cryptography==50.0.1
Flask==3.1.3
google-genai==2.17.0
imageio-ffmpeg==0.6.0
requests==2.34.2
