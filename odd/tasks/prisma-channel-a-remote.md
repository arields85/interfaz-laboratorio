# Prisma Channel A remote — ODD tasks

## Status and next action

SESSION PAUSED by user after the final read-only mapper. RCA-1–4 independently accepted offline and locally committed: e32bef1,0fdfe0f,51ecd70,d2fc349,42edf8b,465321b,f3ac2cb,713f7ae; detailed evidence below. RCA-5 has NO source edits or writer launch. RCA-6–8 not started; full Channel A remains incomplete and unwired. Original baseline6e05e38, branch feat/prisma-telegram-credentials. Final collision recovery mu9ppr3a-17-39bt completed; its proposed hook is NOT an approved implementation. Resume RCA-5 from the checkpoint below. Isolated unittest supervisor remains authoritative, not a mapper's pytest suggestion.

Recovery mirror: `odd/prisma-channel-a-remote/tasks` (full document plus this path).

Latest user instruction SUPERSEDES the earlier complete-RCA-5-before-closing boundary: close after the last agent and resume RCA-5 next session. No more agents or implementation now. Record a local documentary checkpoint, no push. RCA-5 remains incomplete; RCA-6–8 not started.

## Session closure and exact return point

Last accepted code commit: `713f7ae1bd9fece599b0281c4a1e7adb105c1b86`. This closure changes only this tracker; actual final documentary HEAD is recorded in Engram after commit, avoiding a postcommit tracked edit. No new tests: latest historical independent evidence is backend625 (RCA-4b), frontend1966/202files plus coverage/build/lint (RCA-4c). No operational/provider acceptance.

Resume by reading this file/full Engram mirror, checkpoint/prisma-channel-a-implementation-progress, master §11.1 and active PW-003 detail, then confirm Git state. First resolve authorization/priority for cooperative A/B bot identity exclusion; do not silently expand B. Review the prepared RCA-5a transport contract and freeze the exact two-file delegated RED-first scope before launching its writer. It is independently implementable, but no writer or transport files were created this session. Then finish known-view invalidation analysis and bounded RCA-5 policy/lifecycle/HTTP/admin/proxy/scheduler/bootstrap units, each independently verified before local commit. No RCA-6 until RCA-5 actually closes.

Final collision mapper: `mu9pi51p-16-u9uo` failed generically with no usable result/cause; narrowed retry `mu9ppr3a-17-39bt` completed read-only. B operation_lock serializes apply/save/delete/stop, but public nine-field status/generations contain no authoritative bot identity. A-only checks cannot exclude concurrent B apply to the same bot. Separate credential rows, token-prefix assumptions and historical IDs do not solve this. A shared identity reservation requires B participation and may reject a previously permitted B apply: permission and priority policy remain OPEN. A must never stop B or inspect its private persisted authority.

The mapper suggested reservation post-prepare/pre-start. This is a PROPOSAL, not accepted safe design: audit prepare for provider/persistence effects and reserve before conflicting effects; retain reservation on unconfirmed stop/cleanup, bind release to the correct lifecycle owner/epoch. Preserve B admission, JsonFileStore and nine-field status. No hook/code or product-priority decision is authorized by this checkpoint. Known-invalid visible-view invalidation is also unresolved: presence polling and a15s TTL alone do not prove the old view remains visible. No new heartbeat policy approved.

## Objective and authority

Let a person viewing an inputless HMI use their phone to ask about that document's current visible snapshot. Source of approved intent: `docs/prisma/PRISMA_DOCUMENTO_MAESTRO.md` v2.0.12 §11.1 and `checkpoint/prisma-channel-a-remote-odd`. User explicitly authorized implementation with ODD, a maintained frontend QR dependency/install, and future feature-branch-chain review slices. No SDD, push, PR creation, live Telegram/Gemini calls, paid tests or runtime restarts are authorized. User authorized local code/test/tracker commits after each verified work unit, beginning with RCA-1; no push or PR.

## Contract and constraints

- Dedicated bot A; preserve bot B behavior, admission, lifecycle and persisted snapshot. Reuse the protected credential store and existing admin session; no second login.
- Automatic single-use opaque QR, rotating every 60 seconds while free. Never expose HMI capability/admin/bot secrets. Phone confirms destination with Telegram buttons; Start may be required. QR possession is not proof of proximity; forwarded-photo risk remains a design limitation, not a waived security requirement.
- One phone per HMI document session and one HMI per phone; multiple independent pairs. No takeover/implicit transfer. Hide QR while linked; phone unlink. In-memory links expire on restart/reload/session expiry.
- Human inactivity is 10 minutes, independent of technical polling. Advance warning and keep-connected; user-approved existing-admin configuration persisted in runtime, default60s/range15–300s. Presence-loss cutoff30s and context freshness15s are approved, distinct from human idle.
- Existing deterministic visible-snapshot parser only. Same answer text in HMI and phone, existing HMI audio. No Telegram response audio or latent recipient path. STT/NLU/navigation/history/offscreen data are deferred. No industrial writes.
- Server-observed context receipt freshness and bounded presence, never client timestamp alone or stale/global/cross-owner fallback. Internal owner lookup never provides a capability and never renews session activity.
- Generation/correlation protects late answers/audio across unlink/relink, including an unchanged session owner. Deduplicate updates; offsets alone do not guarantee exactly-once.
- Frontend domain types only in `domain/`; tokens, Lucide and `hmi-scrollbar`; follow project admin conventions. No edits under `Directrices/`.

## Tasks and boundaries

All tasks use one delegated writer at a time (multi-file/preparation trigger). About 400 authored lines per task is advisory only: do not drop tests, minify or split artificially to fit.

- [x] **RCA-1 — Fresh internal HMI context (offline verified; committed e32bef1).** Add server receipt time and internal owner-scoped freshness access without changing capability callers. Exact source scope: `services/prisma-runtime/src/prisma_runtime/hmi_sessions.py`; tests: `services/prisma-runtime/tests/test_hmi_sessions.py`. Prove missing/stale/closed/expired rejection, independent owners, deep-copy isolation, no bearer leakage and no activity renewal. Explicit positive finite freshness bound supplied by caller; no arbitrary product default. Preserve existing routes/behavior. Forecast 120–250 lines.
- [x] **RCA-2 — Pairing domain (offline verified; committed 0fdfe0f).** New `channel_a_pairing.py` and `test_channel_a_pairing.py`: bounded token lifecycle, atomic claim/phone confirmation, exclusivity, generation, unlink, human timeout/warning/keepalive. Fake clocks, entropy, races and late-result tests. No HTTP/providers. Depends RCA-1. Forecast 350–650.
- [x] **RCA-3 — Dedicated bot adapter (3a/3b/3c committed, verified offline).** New bot A module/tests: private chat identity, callbacks, confirmation, unlink/keepalive, text-only answers, dedupe and correlated cancellation. All transport mocked; no B behavior changes. Depends RCA-2. Forecast 300–550.
- [x] **RCA-4 — Protected A credentials/admin contract (4a committed465321b; 4b committedf3ac2cb; 4c committed713f7ae).** Protected `telegram_channel_a` provider, standalone resolver, exact domain parser and credential admin card in existing session. Preserve B status contract. Runtime status/apply/lifecycle integration belongs explicitly to RCA-5; do not present unwired readiness in RCA-4. Forecast 400–750, revised per subunit.
- [ ] **RCA-5 — HTTP/runtime integration (planning).** Capability-scoped pairing endpoints/proxy, internal freshness and generation gates, dedicated A admin runtime status/apply, lifecycle startup/shutdown, bot username discovery via mocked getMe; unconfigured/stopped bot yields unavailable state, not a fake QR. Configure bounded warning/presence policy. Integration/negative isolation tests. Depends RCA-1–4. Forecast 400–750.
- [ ] **RCA-6 — HMI QR/link presentation.** Select maintained local QR encoder (no external QR service), pinned lockfile; session client, typed pairing state, hook and automatic UI. Token expiry/reset/error and multi-HMI tests. Depends RCA-5. Forecast 350–650 excluding generated lockfile.
- [ ] **RCA-7 — HMI text/audio correlation.** Visible text plus existing audio, generation invalidation cancels stale work; phone receives matching text. Resolve automatic transcript placement/lifetime before UI implementation; no required local interaction. Component/service tests. Depends RCA-5–6. Forecast 250–500.
- [ ] **RCA-8 — Integrated verification and docs.** Backend/HMI full suites, coverage/build/lint, independent risk-based check; README/master reconciliation. Keep offline vs live evidence distinct and pending backlog active. Forecast 100–200.

## RCA-5a prepared contract — NOT started

Proposed exact new paths under `services/prisma-runtime/`: `src/prisma_runtime/channel_a_transport.py` and `tests/test_channel_a_transport.py`; both absent at mapping. No writer launched, source edits or dependency installation. Revalidate technical details before freezing the writer scope. ChannelATransport uses fixed official HTTPS Bot API, caller-supplied finite positive request timeout and lazy per-call owned session factory (explicit None default). No shared session/global lock across long poll and sends; no I/O at import/construction, retries, redirects, webhook mutation, deeplink helper, env token fallback, polling loop or lifecycle wiring.

send_message/answer_callback_query match existing raw-body Protocol; 2xx mapping passed through, 4xx only with explicit ok:false passed through for adapter classification; all3xx/5xx, contradictory success, malformed JSON/status/body and dependency failures become fixed UNAVAILABLE from None. Never copy B raise_for_status-before-parse behavior. Attempt response/session close once each even on failure; cleanup exceptions never mask observed result/primary error or leak canaries. No logging/repr of token/URL/body. Per-call sessions are caller-factory owned by transport until close; fake sessions in every test.

get_me returns frozen ChannelABotIdentity(id,username): positive nonbool ID within existing MAX_TELEGRAM_ID, is_bot true, ASCII letters/digits/underscore username5–32 (official public BotFather documentation), no suffix assertion. get_updates takes integer nonnegative poll_timeout, finite read_timeout strictly greater, optional nonbool offset0..MAX_TELEGRAM_ID+1; exact allowed_updates message/callback_query, limit100; returns ordered tuple of raw mappings, no offset advancement/update-id validation. Fixed input validation before I/O: private positive chat, send text1–4096 chars, callback ID/ack bounds reuse adapter constants, omit optional None fields. Token is untrimmed ASCII path-safe letters/digits/underscore/hyphen/colon, bounded by existing MAX_SECRET_BYTES, no assumption that its prefix proves bot identity. Default requests session disables ambient auth/proxy via trust_env=False, TLS verification remains default; no custom API host.

Strict RED-first and deterministic fake-session tests for classification, identity, params, laziness/owned sessions, no retry/redirect, exact resource closure and sanitized errors. Exact isolated focused supervisor pattern test_channel_a_transport.py; full README offline gate. Forecast450–750 authored source/test lines, advisory. Public docs read only: https://core.telegram.org/bots/features#botfather confirms username5–32 and deep-link parameter alphabet/64char bound; not a bot API call. No real provider/testing calls. Parent assessment/independent verification required.

## RCA-5 planning evidence

Map completed: standalone A transport/identity first (`channel_a_transport.py` plus test), then policy/collision safety, lifecycle, coherent HTTP/admin/proxy contracts, scheduler/owner-invalidation wiring and integration tests. These are provisional units, not approved source scope; original400–750 forecast must be replaced per unit (transport revised450–750 source/test lines). Reuse B HTTP pattern only, never B persisted offsets/single-chat admission/JsonFileStore. Capability-authorized HMI owner comes from session_owner(), not request UUID. getMe validates A bot identity/username before any usable QR. Closed/reloaded/expired owner and stopped/reconfigured A revoke linking authority; publisher revalidation remains RCA-7.

User approved: warning configuration in existing admin panel, persisted in runtime; default60s anticipation, configurable15–300s; invalidate link after30s without HMI presence (explicit close/revoke immediately). FixedQR60s/humanidle600s remain. User subsequently approved freshness15s after cadence evidence: viewer exporter rebuilds/posts even unchanged frames every5s nominal, timeout4.5s, single-flight skipped ticks; hidden/offline/navigation gaps have no finite guarantee. Voice polling every1s renews owner presence only, not context freshness/human idle. Reject beyond15s; never reuse known-invalid/offscreen context merely because its age is belowTTL. No new heartbeat interval approved. Transport map `mu9pi4yh-15-4wq0` completed read-only; narrowed collision map completed as recorded above. Known-view invalidation seam still needs analysis next session. Sessionidle30min and audioTTL300s are NOT freshness policy; do not conflate presence with human idle. Answer bound4096 Telegram characters is a protocol candidate, to validate before transport contract. Collision proposal comparing B live/historical IDs is incomplete: it misses unobserved desired identities and concurrent later B reconfiguration; historical IDs can over-reject. Do not accept it as sufficient or inspect B private/persisted authority unsafely. Resolve a synchronized fail-closed seam without silently expanding B lifecycle behavior before runtime wiring. No live token/getMe/provider calls authorized.

## RCA-4 planning evidence

Backend read-only map: `credential_store.py` ALLOWED_PROVIDERS drives generic store validation/status and admin GET/PUT/DELETE. Additive `telegram_channel_a` row requires no SQLite schema/DDL/version migration; existing databases remain valid. Older binaries reject unknown provider rows, so downgrade after writing A requires an explicit safe rollback plan, not a blind binary rollback. Generic admin routes already preserve B manager special-case only for provider `telegram`; do not add fake A running/apply/verified status.

Frontend exact parser/union in `domain/adminCredential.types.ts` rejects third metadata key/provider echo; backend and parser must change in one coherent slice. All affected strict typed records/fixtures must update too. Parent rejects mapper suggestion to defer `test_credential_http.py`: backend HTTP mocks/expected bodies must be updated in the same first slice so full gate stays green. Frontend consumer/fixture map completed before the ten-path writer scope was frozen. Dedicated A resolver later uses protected store ONLY, no B/env fallback, same admin session. UI card follows necessary parser/contract compatibility; neither resolver nor card is implemented in RCA-4a.

## RCA-4c accepted contract

Full execution scope/commands preserved at `713f7ae:odd/tasks/prisma-channel-a-remote.md`; evidence below remains unchanged. Eight-path component/proxy/test/routing-doc unit; no backend/hook/service/domain production changes. Three provider-keyed drafts, functional clearing, revision/panel/session/dialog fences, complete deactivation/logout reset and no persistent/cache/log secrets. A fieldset Telegram (Canal A), unchanged B label/status/apply and Gemini text, provider-specific deletion confirmation. Saving A does not start/verify it. Exact protected A credential PUT/DELETE proxy preserves queries/cookies/CSRF, strips HMI capability and rejects wrong methods/encoded lookalikes/suffixes; no runtime/pairing/apply routes. Strict TDD plus six-file focused suite (including root proxy), full coverage/build/lint independently passed; tests preserve race/draft isolation and metadata-only semantics. Original forecast300–500; actual518, not reduced for budget.

## RCA-4c evidence (accepted offline)

Writer reports RED13fail/89pass across102 tests in3failingfiles; GREEN102/6focusedfiles (root proxy observed), fullcoverage1966/202files, build/lintPASS. Coverage lines88.05%(12836/14578), branches80.23%(10024/12493), functions86.32%(3213/3722), statements87.16%(13588/15588), all70thresholds passed. Hook/service A pass-through tests were already green, not RED. Exact8candidatepaths422add96delete=518authoredlines,18overforecast; preserve tests/readability despite JSX reindent. No backend/hook/service/domain production changes. Parent read full component and proxy/doc diffs and ran whitespace/scope checks. Native assess schema-incompatible/unassessable=>independentrequired. Verifier `mu9lw32v-12-spat` active for samefreshfrontendgates and per-provider secret/late-result/proxy/docboundary review; parent explicitlyowns trackerupdates throughout. Final independent PASS: focused102/6files (twice, proxy execution confirmed), fullcoverage1966/202files with the same percentages/counts, build/lint and whitespace checks. Full patch and all eight before/after content hashes stable; no candidate blocker. Parent rechecked all matching hashes and diff whitespace. RCA-4c accepted offline and committed locally as `713f7ae` (450 additions+144 deletions across eight candidate paths plus tracker consolidation). Runtime/live forwarding remains unverified and unwired. Parent consolidated closed RCA-2/3 execution contracts to repair the approximately50k Engram readback limit, retaining detailed evidence and full prior contracts in Git f3ac2cb; full current mirror restored and read back without truncation.

## RCA-4b accepted contract

Full scope/commands preserved at `713f7ae:odd/tasks/prisma-channel-a-remote.md`. New runtime `channel_a_credentials.py`/test only. `ChannelACredentialResolver(credential_service_factory=None).resolve() -> str`, `ChannelACredentialError` with fixed PRISMA_CHANNEL_A_CREDENTIAL_MISSING/UNAVAILABLE. Explicit None selects lazy default factory; constructor does no I/O. Default uses existing runtime_paths/permissions/store/master-key config, no token env source. Every call rereads telegram_channel_a; preserve valid bytes, no cache. None/blank/nonstring means MISSING outside catch; factory/store/presence exceptions including same-class canaries normalize to UNAVAILABLE from None. Store owns validation. Missing master never authorizes fallback. Tests use synthetic patched composition, real empty-key rejection with I/O guards outside catch; no machine state. Isolated14/full625 gates independently passed, frontend unchanged. Forecast175–255; actual318. Writer recovery and RED provenance below retained.

## RCA-4b evidence (accepted offline)

Failed task returned only assistant-reported error, no cause/gate evidence. Parent observed exactly two new untracked authorized files plus its tracker, HEAD465321b unchanged, and read both files fully. Resolver and13 test methods exist; file presence does not prove RED/GREEN or completion. Native assess returned empty-output/unassessable, so independent verification remains required. Same-writer continuation must recover actual prior observations without fabricated/recreated RED or replaying uncertain commands, then complete exact isolated focused/full gates. Parent requested durable missing-master→UNAVAILABLE coverage with synthetic paths and hard I/O guards; current configured-master/missing-secret tests alone do not cover that boundary. No code acceptance/commit; no real-store/provider execution authorized.

Recovery writer reports retained original missing-module RED, corrected test-harness call-tree assertion failure, and raw presence.strip canary RED before moving presence computation inside try. Aborted-turn cause remains unknown; every invoked command reportedly returned, no unfinished execution. Fresh focused14/fullbackend625 PASS; missing-master test passed existing code, additional coverage not a RED bug. Exact two new files:50source+268tests=318 authored lines, no existing-source changes. Whitespace read/grep covered untracked files; tracker changes parent-owned. Forecast175–255 exceeded without trimming tests. Native reassessment again empty-output/unassessable. Independent verifier `mu9l17wp-z-k8db` active: full new-file readback, exact isolated focused/full backend gates, missing-master I/O-guard and boundary scrutiny; no frontend rerun or live acceptance. Final independent PASS: focused14/fullbackend625, complete new-file readback, unchanged source/test hashes, no whitespace issues or extra source edits. Missing-master test exercises real empty-path rejection and verifies I/O mocks outside the resolver catch. No further probes needed. Parent rechecked matching hashes and read the final missing-master test. RCA-4b accepted offline and committed locally as `f3ac2cb` (336 additions+4 deletions, two new source/test files plus tracker). Frontend evidence remains from RCA-4a, not rerun; no integration/live acceptance.

## RCA-4a accepted contract

Full ten-path scope/commands preserved at `713f7ae:odd/tasks/prisma-channel-a-remote.md`. One coherent backend provider-registry/frontend strict three-key parser rollout plus typed UI revision initializer and affected fixtures. Existing two-provider databases valid without DDL/version change; older binaries reject stored A row. Same admin auth/CSRF, B manager special-case/status unchanged. A uses only generic protected store; missing/unknown/nonboolean metadata rejected, no secret echo/plaintext storage and AAD prevents provider relabeling. Initial HTTP test guard was ineffective; corrected actual mock_calls after PUT/DELETE and independently injected side effects proved it. Pre-existing unrelated guards untouched. Final source/test320 lines (forecast150–350); independently verified focused/full backend and frontend coverage/build/lint. Detailed RED, correction and gate evidence below unchanged.

## RCA-4a evidence (accepted offline after guard correction)

Writer-reported RED: store10 tests/4 failures+2errors, HTTP9/2failures; focused frontend68/9failed. GREEN: store10, HTTP9, frontend68; full backend611, frontend1950 in202files, build/lint PASS. Exactly ten authorized source/test paths,288 additions+31deletions=319 authored lines. No coverage run by writer. Parent observed exact scope and production diff (provider tuple, strict parser/union, typed UI initializer only), and clean diff whitespace check. Native ASSESS unavailable: schema-incompatible response, treated high-risk; no native review started. Independent verifier now running focused/full backend, frontend focused/full coverage70/70, build/lint plus boundary inspection/probes. Results and RED remain writer-reported until separately observed; RCA-4a unaccepted/uncommitted. No migration, A resolver/card/runtime integration/provider delivery yet.

Independent final evidence: store10/HTTP9/full backend611, frontend focused68/5files and full coverage1950/202files PASS; build/lint PASS. Coverage lines87.97% (12820/14572), branches80.17% (10019/12497), statements87.09%, functions86.22%; all four enforced70 thresholds passed. Canvas getContext, unresolved /grid.svg and >500kB chunk advisories reported. Parent approved exact q2 pure-memory Flask probe once:20 requests PASS, no route defect observed. Probe also reproduced a durable-test gap: new HTTP test line200 uses parent Mock.assert_not_called(), which ignores calls to child methods. Same pre-existing credentials-mock weakness at78/171 is separate. Final verdict FAIL solely on the new durable regression guard; no production route defect. Parent confirms excluded tracker changes19/5→23/5 were its own evidence edits. Exact ten source/test candidate paths stayed288/31; verification finished before correction writer launch. Full gates passing does not waive this regression-protection gap.

Bounded correction: only new A-write test in runtime `tests/test_credential_http.py`; assert actual `telegram_manager.mock_calls == []` after PUT and after DELETE. Leave pre-existing credentials assertions78/171 untouched. Parent supplied/authorized an exact isolated in-memory mutation supervisor: inject synthetic B method calls via each generic-store mock, expect original actual test to miss both (RED), corrected guards detect both (GREEN); no source mutation by the probe. Writer may run this exact command once before and once after, then canonical HTTP/full backend gates. No frontend/source changes; fresh native assess and independent recheck required afterward. Forecast3–6 authored correction lines.

Correction writer reports exact mutation RED exit1/missed2 before edit, GREEN exit0/both methods caught afterward; HTTP9/fullbackend611 PASS. Only target test changed: two actual mock_calls guards, incremental+2/-1=3 diff lines (not worker's claimed2); final candidate289add31delete=320 authored lines. Pre-existing assertions untouched. Fresh native assess again schema-incompatible/unassessable, so independent recheck required. Reverifier `mu9k1b0z-v-y8nz` has exact one-shot corrected-test mutation probe plus fresh HTTP/fullbackend/frontendfocused/fullcoverage/build/lint gates authorized. Parent supplied tracker attribution for late q3 via continuation after direct reply returned query unavailable. Final independent PASS: both PUT/DELETE guards caught injected B-method calls; HTTP9/fullbackend611/frontendfocused68/fullcoverage1950 in202files/build/lint all freshly passed. Coverage unchanged: lines87.97%, branches80.17%, statements87.09%, functions86.22%, every70% threshold passed. Full patch compared with original, not merely numstat: only intended +2/-1 correction differed. No remaining candidate blocker, tracker attribution resolved. Parent read both final guards and reran git diff --check. RCA-4a accepted offline and committed locally as `465321b` (318 additions+36 deletions across ten source/test files plus tracker). Resolver/card/proxy/lifecycle and live acceptance remain pending.

## Accepted RCA-2/3 integration contracts

The full prior execution contracts remain in Git at `f3ac2cb:odd/tasks/prisma-channel-a-remote.md`. This consolidation retains integration invariants; detailed RED/correction/acceptance evidence below is unchanged. It keeps the complete current document mirror below Engram's observed approximately50k readback bound. No source or acceptance scope changes.

- **RCA-2, pairing:** `channel_a_pairing.py` plus its colocated runtime test module; bounded, locked in-memory registry with injected monotonic clock/entropy and immutable snapshots. One60s opaque challenge per free owner; polling never extends TTL. Claim reserves a phone/owner pending explicit confirmation, whose deadline cannot exceed the original QR. Pending and linked exclusivity prevent takeover/transfer/replay. Phone identifies a validated private Telegram actor, not hardware. Opaque tickets and generation bind actions; owner ID alone never authorizes. Confirm starts600s human idle; only admitted human interaction renews. Expiry rejects at equality; unlink/owner invalidation clears associations/challenges. Finite arithmetic, regression watermark and sampling under lock are mandatory. No unbounded tombstones or production storage.
- **RCA-3a, private dialogue:** `channel_a_bot.py` and tests. Inject registry, configured bot identity, current/fresh destination-label reader and text transport. Revalidate the exact displayed label before confirmation. Reserve adapter capacity before claiming; never evict live pending authority to admit an unauthenticated replacement. Private chat/sender/bot-message identity and supported update shape must agree; reject bool IDs, ambiguous inline/business/guest/sender_chat/via_bot inputs. Strict `/start`/callback grammar,64-byte callbacks, separate confirm/cancel and process-local opaque action nonce for exact phone/owner/generation. No token in text/log/errors/repr outside intended callback/deep-link fields.
- **Ingress/output authority:** serialize adapter ingress/effects, maintain update high-water/confirmation fences and a fresh adapter epoch. Old/stale/duplicate inputs never replay or retarget. After Telegram's week-idle update-ID discontinuity, start a fresh adapter/empty-link epoch, not a reset retaining authority. No durable B offset/retry copying or exactly-once claim. Validate send receipts' positive ID/private destination/bot origin; outcomes delivered/rejected/unknown, no retry on unknown. Acknowledgement cannot authorize. Uncertain registry/clock failure preserves live tickets/actions; authoritative expiry/release removes them. No domain lock across external callbacks/I/O; revalidate generation immediately before effects without pretending network atomicity or retractability.
- **RCA-3b, captured queries:** `channel_a_query.py`/tests with narrow adapter integration. `enable_queries` is one-shot startup attachment. Capture immutable owner/phone/generation/update/adapter epoch at admission; no lazy lookup of a replacement destination. Inject existing `answer_from_snapshot`/owner context, never global/B fallback or duplicated parser. Question bound4096UTF8bytes; keep distinct128-character `/start` limit. Server receipt age plus monotonic elapsed time anchor freshness before acquisition; new snapshots never extend an existing answer's deadline. Finite bounds/ages/arithmetic and regression checks required. Effectful admission validation runs BEFORE authoritative registry check; resample freshness immediately before output. Human touch only for admitted human input. Fail closed on malformed/throwing parser results without payload leakage; plain bounded text, no silent truncation. Envelope carries identical phone text only after observed delivery plus post-send validity. Future HMI publisher must revalidate it again; publication/audio remain unwired.
- **RCA-3c, inactivity:** shared atomic `warning_due`/`due_warnings` reservation under domain lock, bounded immutable snapshots, no technical renewal. Explicit serialized adapter sweep rechecks own admission/nonce and exact owner/generation/activity/deadline window per recipient, including mutations caused by earlier recipients. Stale/expired/replaced/refreshed/uncertain snapshots skip without destroying uncertain controls. Reuse Spanish Seguir conectado/Desvincular buttons and existing text-send classification. Reservation is one attempt, not delivery: rejected/unknown/skipped do not rearm/retry until human activity. Return no answer envelope; scheduler belongs to RCA-5. Warning lead stays injected (positive and below600s) until configured policy is settled.
- **Protocol evidence:** only public `https://core.telegram.org/bots/api` was read, never bot endpoints. Sequential update IDs can randomize after at least one idle week; higher getUpdates offset acknowledges updates retained at most24h. Callback messages differ from inline callbacks; callback_data is1–64bytes; sendMessage success returns Message. These facts do not establish universal exactly-once or authenticity outside trusted transport.
- **Tests:** corresponding runtime pairing/bot/query modules cover boundaries, races, replay/foreign identity, owner invalidation, same/different-owner replacement, restart epochs, refreshed contexts, delayed validators/parsers/transports, short remaining QR lifetime, uncertain outcomes, capacity, warning windows and no retry. Use the unchanged isolated supervisor below with each exact test pattern; preserve all accepted regression coverage. Runtime/UI/provider integration remains pending, not inferred from these offline units.

## TDD and verification

TDD ON for logic/services/bugs, mandated by `AGENTS.md` §8–9 and `docs/TESTING.md`; observed RED before source edits, GREEN then refactor. Components also receive behavioral tests. No historical suite count is current evidence.

Frontend runner: `npm --prefix hmi-app test -- <focused paths>` (`vitest run --allowOnly=false`); integration: `npm --prefix hmi-app test`, `npm --prefix hmi-app run test:coverage`, `npm --prefix hmi-app run build`, `npm --prefix hmi-app run lint`.

RCA-1 focused command, from repository root in Bash/Git Bash (repeat for RED/GREEN):

```bash
./services/prisma-runtime/.venv/Scripts/python.exe -B - <<'PY'
import os
from pathlib import Path
import subprocess
import tempfile
with tempfile.TemporaryDirectory(prefix='prisma-rca-test-') as temporary:
    env = os.environ.copy()
    for name in list(env):
        if name.startswith(('PRISMA_', 'TELEGRAM_', 'GEMINI_')):
            env.pop(name)
    env['PRISMA_RUNTIME_STATE_DIR'] = temporary
    env['PYTHONPATH'] = str(Path('services/prisma-runtime/src').resolve())
    result = subprocess.run([
        'services/prisma-runtime/.venv/Scripts/python.exe', '-B', '-m',
        'unittest', 'discover', '-s', 'services/prisma-runtime/tests',
        '-p', 'test_hmi_sessions.py',
    ], env=env, check=False)
raise SystemExit(result.returncode)
PY
```

RCA-3b focused commands: use the exact RCA-1 supervisor above for each of `test_channel_a_query.py` and `test_channel_a_bot.py`, changing only discovery pattern; same full isolated gate.

RCA-3a focused command: use the exact RCA-1 supervisor above with only the unittest discovery pattern changed to `test_channel_a_bot.py`; same full isolated backend gate and RED/GREEN discipline.

RCA-2 focused command: use the exact RCA-1 supervisor above with only the unittest discovery pattern changed to `test_channel_a_pairing.py`. Observe RED before production implementation. Run the same isolated full backend gate after GREEN. Its caches are authorized incidental test output, not source changes.

RCA-1 full backend command: execute the exact child-only supervisor code block under **Offline-safe verification** in `services/prisma-runtime/README.md` (owned Python -> `operations/verify-local.ps1`, fresh temporary state, eleven sensitive/config overrides removed). Inspect fixtures before running; never invoke the production launcher or bare unisolated gate. This is environment isolation, not a network sandbox. Stop unexpected live access. Later A-specific env overrides must also be removed when introduced.

Every writer reports exact command/result, RED failure reason, GREEN counts, changed paths and limitations. `git diff --check` required. Parent calls native `assess` after each writer; failed/unavailable assessment treated as high with independent verifier. Parent spotchecks one reported command before delivery. No review/consent authority inferred from checkboxes.

## RCA-3c evidence (accepted offline)

Writer reports registry RED53 (missing due_warnings) thenGREEN54; adapter missing-constantRED thenGREEN180; full606PASS. Exactly registry/adapter and corresponding tests changed,611add+8delete=619authoredlines, tracker untouched by writer. Public due_warnings reserves once/window under domain lock; adapter sweep rechecks exact activity window/admission per recipient and reuses current nonce controls. No scheduler/runtime wiring. Native assess now succeeded: high, process_boundary/shell_process signal on adapter, RDDoff; this is assessment classification, not evidence of executed shell/provider activity. Independent PASS:54registry/180bot/full606 and12 pure-memory probe groups observed, no blockers. Rechecked expiry/same-generation refresh/new reservation/replacements, no domain lock across fake I/O, uncertainty preserves controls. Parent read domain reservation/adapter send paths and reran git diff --check. Best-effort single-attempt policy explicit; no automatic retry/rearm on failed/unknown/skipped send. RCA-3c accepted offline, committed locally as `42edf8b` (628 additions +13 deletions, exact four source/test files plus tracker); RCA-3 now complete as offline logic, not runtime/live integration.

## RCA-3b evidence (accepted offline after correction)

Writer reports RED for missing query module/adapter constant, GREEN62 query +155 adapter, full558 backend. Exactly four allowed source/test paths changed: new coordinator511lines/tests834; adapter194add/21delete; adaptertests305add/1delete. Source/test authored total1,866lines. Optional `enable_queries` attachment is now explicitly startup-only: single successful attachment per adapter, repeated attempts rejected before mutation. No hot-reconfiguration framework. Widened Action-field assertion independently confirmed additive. Over-limit answer refusal instead of truncation matches approved contract. Envelope latent, only returned on observed phone delivery plus post-send binding/freshness validity; no HMI publication. Independent verdict FAIL despite observed62query/155bot/full558 PASS. HIGH: registry read before effectful validator authorizes stale phone output, old-generation envelope and unbound notice. HIGH: final validator advances time past captured deadline before phone send. MEDIUM: accepted enable_queries replacement leaves old in-flight coordinator authority. LOW: answer_text property exception escapes parser normalization. Nine pure-memory cases: eight reproduce failures, one confirms new-adapter rejection. Bounded writer correction observed queryRED74 (five failures/one error) thenGREEN74; adapterRED158 (three failures) thenGREEN159; full574PASS. Callback-first/registry-after and final freshness resampling implemented, one-shot startup attachment serialized, accessor failures normalized. Correctedcandidate522query+1078tests+204/20adapter+393/1adaptertests=2218authoredlines. Fresh independent PASS:74query/159bot/full574 and14 pure-memory probe cases/groups observed. All four findings closed, no new blocker; no expired answer/stale envelope/unbound notice, single-attachment policy preserved, accessor canary normalized. Parent read final binding/freshness paths and reran git diff --check. No scope expansion. Native assess unavailable/RDDoff with writer profile unknown ->independent required. RCA-3b accepted offline and committed locally as `d2fc349` (2,219 additions +25 deletions, exactly four source/test paths plus tracker). No HMI publication/runtime/provider acceptance, RCA-3c still pending.

## RCA-3a evidence (accepted offline after correction)

Writer created only new `channel_a_bot.py` (935 lines) and `test_channel_a_bot.py` (1,705 lines): 2,640 authored lines. Reported observed RED missing module, then focused121/full462 PASS. No existing source/B changes. Parent read claim/confirm/purge logic and flagged explicit contract deviations: no comparison against the label displayed before confirmation; oldest live pending metadata evicted before authenticating a new claim; 2**63 ID bound instead of2**53-1 and update0 rejected. Independent verdict FAIL despite observed focused121/full462 PASS;12 pure-memory probe groups reproduced: HIGH changed-label confirmation (and destructive plain-label sanitization), HIGH eviction of live pending authority before admission, MEDIUM callback date/nested ambiguity missing, MEDIUM stale welcome after ack-triggered replacement, MEDIUM wrong ID bounds/update0 rejection, MEDIUM clock/refusal uncertainty falsely reporting cancellation or erasing live buttons, LOW claim-time+TTL instead of authoritative pending expiry, LOW raw-vs-encoded nonce digest mismatch enabling deterministic collision overwrite. Existing tests encode two wrong behaviors and must be corrected to fail first. Fresh writer corrected the same two files, observed RED131 (25 failures across19 methods), then focused131/full472 PASS. Corrected candidate999source+1888tests=2887lines. Stored displayed label/authoritative expiry, pre-admission capacity, callback/ID checks, generation recheck after ack, uncertainty-preserving state and consistent nonce digest reported. One deadline test passed old code too; verifier must probe actual short-remaining QR lifetime. Independent re-verification observed focused131/full472 PASS but verdict FAIL: original1–5/7/8 closed;6 partially open. Confirm clock failure still forgets a valid local ticket, and clock failure during human_touch/unlink still discards live action controls. Additional LOW: entropy hook can invalidate owner before success acknowledgement; post-ack check only suppresses welcome. Bounded follow-up writer observed RED136 (four failures), then focused136/full477 PASS; corrected domain-error classification to preserve uncertain pending/action state and added post-nonce/pre-ack revalidation. Candidate1,030source+2,029tests=3,059lines. Same-ticket/nonce recovery and authoritative stale cleanup tested. Final independent verdict PASS: focused136/full477 and seven residual probe groups observed, all eight original findings plus pre-ack finding closed. Same delivered ticket/nonce recovery, authoritative stale cleanup, same-owner/different-owner replacement and short-remaining QR expiry confirmed. Parent read residual mutation/pre-ack paths and reran git diff --check. RCA-3a accepted offline only; not fullRCA3 or live integration. Short remaining lifetime issue1000/claim1059/purge1060 independently passed. Verifier's first probe heredoc had SyntaxError before execution, then split pure-memory probes succeeded; no mutation. Do not equate green suites with acceptance. Size greatly exceeds forecast; do not remove coverage for line savings. Native assess unavailable/RDDoff -> independent verification mandatory. RCA-3a local commit `51ecd70` contains exactly adapter, tests and tracker (3,086 additions + 5 deletions); RCA-3b/3c source untouched. Candidate3,059 source/test lines remains a coherent oversized unit; future PR slicing/size exception unresolved and no PR created.

## RCA-2 evidence (accepted offline after correction)

Writer created only the two authorized new files: 566 source + 635 test lines = 1,201 authored lines. Observed RED: missing module before implementation. Writer GREEN: 32 focused and 330 full backend tests; git diff --check passed for tracked changes (untracked source/tests need separate readback). Much larger than forecast; do not trim coverage or formatting for size. Registry provides challenge/claim/confirm/cancel, link lookup, generation validation, human touch, warning, unlink and owner invalidation. Independent verifier interim evidence: focused32/full330 PASS but deterministic event/lock probes reproduce stale pre-lock time admitting QR claim/confirm at expiry, result validation and human touch at idle expiry (reviving the link), and delayed touch overwriting newer activity. Finite backward clock samples also regress activity; extreme finite arithmetic can create infinite deadlines; oversized config/clock integers leak OverflowError. Final independent verdict FAIL: pre-lock time and backward clocks HIGH/blocking; infinite arithmetic/raw numeric overflow and inaccurate doc claims LOW. Correction stays within the same two files: monotonic default, clock sampling plus regression checks inside serialized transitions, finite computed deadlines validated before credential consumption/activity mutation, normalized overflow and deterministic race/regression tests. Clarify clock-domain timestamps (not Unix epoch), UUID shape is not authority, and injected clock/entropy are trusted infrastructure functions distinct from forbidden lifecycle callbacks. Writer correction observed RED42 (9 failures/4 errors), then focused43/full341 PASS. Revised candidate: 607 source + 901 tests = 1,508 lines; monotonic default, in-lock time/watermark, finite future deadlines and domain numeric errors implemented. Fresh independent verdict PASS: focused43/full341 observed; deterministic original probes now reject stale/rollback/overflow operations. Pure-memory faulty-order simulation caused four expected assertion failures, demonstrating non-vacuous race tests. All four findings closed, no new blocker. Parent read numeric/deadline/watermark implementation and reran git diff --check. RCA-2 accepted offline, not full feature or live acceptance. Native assessment unavailable again, RDD off: high-risk verification fallback. RCA-2 local commit `0fdfe0f` contains exactly registry, tests and tracker (1,529 additions + 5 deletions). Coherent scope expanded to 1,508 source/test lines; preserve tests and document the overage. Future PR slicing/size exception remains unresolved; no PR authorized or created.

## Delivery and evidence

Strategy: `ask-on-risk`, chain strategy **feature-branch-chain**, selected by user. Original forecast 2,270–4,300 authored lines has already been exceeded; 9,206 lines committed with remaining scopes still pending. Reforecast remaining units before their implementation; generated lockfile excluded. No coverage/formatting reductions to meet obsolete forecasts. Proposed slices: freshness; pairing; bot; protected credentials; wiring; QR; transcript; final integration. Each keeps code/tests/docs together; refine once actual cohesive size is known. Eight authorized local work-unit commits created (`e32bef1`, `0fdfe0f`, `51ecd70`, `d2fc349`, `42edf8b`, `465321b`, `f3ac2cb`, `713f7ae`); no new branch, PR or push. Baseline for new work is `6e05e38`, not the entire historical feature branch. Running committed authored count: 9,206 (RCA-1: 408; RCA-2: 1,534; RCA-3a: 3,091; RCA-3b: 2,244; RCA-3c: 641; RCA-4a: 354; RCA-4b: 340; RCA-4c: 594), including tracker updates. RCA-1 alone: 333 source/test + 75 tracker lines. Smallest current coherent RCA-1 slice is 408 including recovery documentation; keep it intact and resolve any strict PR-size exception before future PR creation, not by shrinking tests or formatting. Final RCA-1 source/test diff: 333 authored lines (332 additions + 1 deletion); tracker additional. Native assessment unavailable (empty native output), RDD off; treated as high for verification, independent verifier required. No native review started.

Evidence: writer observed RED (missing new domain error import), then 17 focused and 294 full backend tests passed. Independent verifier separately observed 17/294 PASS plus pure in-memory probes; parent read the two-file diff. Scope: only authorized source/tests and this tracker. Follow-ups resolved: durable absolute-expiry/refreshed-context/rejected-replacement tests and normalized OverflowError for extreme internal freshness bounds. Follow-up writer observed overflow RED (21 tests, two subtest errors), then focused21/full298 PASS. Independent post-correction verifier observed focused21 PASS and reviewed final diff; full298 remains writer-observed, previous full294 independently observed. Parent read final production change and reran git diff --check successfully. Failed-deepcopy preservation was independently probed, not added as a durable test. RCA-1 accepted offline; no live acceptance or full-feature completion. One verifier command-entry typo failed without mutation, then exact separate Git commands succeeded; no environment workaround. Incidental ignored bytecode caches from the canonical gate explicitly authorized. QR package selection, warning configuration, transcript layout and final integration surfaces remain bounded follow-ups, not permission to invent source paths.
